#include <stdio.h>

int main() {
    int n;
    float sum = 0;
    scanf("%d", &n);

    int a[n];
    for(int i=0; i<n; i++) {
        scanf("%d", &a[i]);
        sum += a[i];
    }

    printf("Average = %.2f", sum / n);
}