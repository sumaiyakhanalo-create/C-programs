#include <stdio.h>
int main() {
    int l,r,i,j,prime; scanf("%d%d",&l,&r);
    for(i=l;i<=r;i++){
        prime=1;
        for(j=2;j<i;j++) if(i%j==0) prime=0;
        if(prime && i>1) printf("%d ",i);
    }
}