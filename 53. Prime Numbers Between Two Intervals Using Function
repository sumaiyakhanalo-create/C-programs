#include <stdio.h>
int prime(int n){
    for(int i=2;i<n;i++) if(n%i==0) return 0;
    return 1;
}
int main(){
    int l,r; scanf("%d%d",&l,&r);
    for(int i=l;i<=r;i++)
        if(prime(i) && i>1) printf("%d ",i);
}